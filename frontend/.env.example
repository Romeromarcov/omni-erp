VITE_API_URL=http://localhost:8000/api

# CxC Lubrikca — allowlist de empresas (UUIDs CSV) que ven el módulo en el build
# 'full'. El admin del sistema (es_superusuario_omni) lo ve siempre. En el build
# standalone 'cobranza' está siempre visible. Vacío = solo lo ve el admin.
# VITE_CXC_LUBRIKCA_EMPRESAS=<uuid-empresa-lubrikca>
